server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/Order_verse
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: ORDER_SERVICE